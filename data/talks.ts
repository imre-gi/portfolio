import type { Talk } from "@/types";

/**
 * Public talks, podcasts, workshops and mentoring engagements.
 * Populate as Imre confirms the public list. Empty array renders the
 * "engagements will appear here" empty state on /speaking.
 */
export const talks: Talk[] = [];
